#define user_length 2000
#define host_length 2000
#define length 2000

typedef long long int ll;

char hm[length]; // home
char prev[length];

char user[user_length], host[user_length];
char his_arr[20][2000];
long long int pid[length];
char proc[100][length];
long long int shravya[length];
char ver [length];
char name[100][length];

extern int frkinp;